#ifndef ECS_H_INCLUDED
#define ECS_H_INCLUDED

#include "../common.h"

/*typedef enum
{
	COMPONENT_NONE = 0,
	COMPONENT_DISPLACEMENT = 1,
	COMPONENT_VELOCITY = 2,
	COMPONENT_APPEARANCE = 4
} Component;

class World
{
    public:
        int getFreeEntity();
        void incrementVectsSize();
        inline void deleteEntity(int index) {mask[index] = COMPONENT_NONE;}

        std::vector<int> mask;

        std::vector<Rectangle> rectangle;
        std::vector<Vect2> velocity;
        std::vector<GfxParameters> gfxParameters;
        std::vector<PhysicalProps> physicalProps;
        std::vector<>
};*/

#endif // ECS_H_INCLUDED
