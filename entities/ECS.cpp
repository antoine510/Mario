#include "ECS.h"


/*int World::getFreeEntity()
{
    for(unsigned int ent = 0; ent < mask.size(); ent++)
    {
        if(mask[ent] == COMPONENT_NONE)
        {
            return ent;
        }
    }
    incrementVectsSize();
    return mask.size()-1;
}

void World::incrementVectsSize()
{
    mask.push_back(COMPONENT_NONE);
    rectangle.push_back(Rectangle());
}*/

